#!/usr/bin/env bash
set -euo pipefail

if [[ "${EUID:-$(id -u)}" -ne 0 ]]; then
  echo "Please run as root: sudo wol-proxy-setup" >&2
  exit 1
fi

APP_DIR="/opt/wol-proxy"
PY_PATH="${APP_DIR}/src"

exec /usr/bin/env PYTHONPATH="${PY_PATH}" python3 -m wol_proxy.setup_tui "$@"
