#!/usr/bin/env bash
set -euo pipefail

if [[ "${EUID:-$(id -u)}" -ne 0 ]]; then
  echo "Bitte als root ausführen: sudo wol-proxy-setup" >&2
  exit 1
fi

exec /usr/bin/env python3 /opt/wol-proxy/src/wol_proxy/setup_tui.py "$@"

